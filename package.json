{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "biuld": "turbo run build"
  },
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
